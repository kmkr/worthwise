import { Fragment, h } from "preact";

const SurveyCreated = () => {
  return (
    <Fragment>
      <h2>Yay!</h2>

      <p>
        You now need to wait a bit. We'll send you an email when everything is
        ready.
      </p>
    </Fragment>
  );
};

export default SurveyCreated;
